
name: 'civic_audit'
version: '1.0.0'
config-version: 2

profile: 'civic_audit'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  civic_audit:
    staging:
      +materialized: view
    marts:
      +materialized: table
